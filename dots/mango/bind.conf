# Key Bindings
# key name refer to `xev` or `wev` command output,
# mod keys name: super,ctrl,alt,shift,none

# reload config
bind=SUPER,0,reload_config
# applications
bind=SUPER,W,spawn,noctalia msg panel-toggle wallpaper
bind=SUPER,BackSpace,spawn,noctalia msg panel-toggle session
bind=SUPER,M,spawn,noctalia msg panel-toggle launcher
 bind=SUPER,N,toggleoverview
bind=SUPER,D,spawn,fuzzel
bind=SUPER+SHIFT,s,spawn,sh -c 'grim /tmp/qs-master.png; qs -c mango ipc call screenshot capture'
bind=SUPER,Return,spawn,ghostty
bind=SUPER,T,spawn,footclient
bind=SUPER,0,spawn,noctalia msg config-reload
# bind=SUPER+SHIFT,S,spawn_shell,wayfreeze --hide-cursor & PID=$!; sleep .1; grim -g "$(slurp)" - | wl-copy; kill $PID
# bind=SUPER+CTRL+SHIFT,S,spawn_shell,grim - | wl-copy | notify-send -i nix-snowflake-white "Screenshot taken" "Grim has screenshotted the full screen"
bind=SUPER+CTRL,S,spawn_shell, wayfreeze --hide-cursor & PID=$!; sleep .1; grim -g "$(slurp)" /tmp/screenshot.png; kill $PID ; swappy -f /tmp/screenshot.png
bind=SUPER+SHIFT,R,spawn,kurukurubar ipc call record start
bind=SUPER+CTRL+SHIFT,R,spawn,kurukurubar ipc call record stop
bind=SUPER,I,spawn_shell,stash list | fuzzel --dmenu | stash decode | wl-copy
bind=SUPER,V,spawn,noctalia msg panel-toggle clipboard
bind=SUPER+SHIFT,space,spawn_shell, fish -c 'set input (history search | fuzzel --dmenu); if test -n "$input"; footclient fish -c "$input"; end'
bind=SUPER,O,spawn,noctalia msg panel-toggle control-center
bind=ALT+CTRL,L,spawn,noctalia msg session lock
bind=NONE,Scroll_Lock,spawn,noctalia msg session lock
# exit
bind=SUPER+SHIFT+CTRL,e,quit
bind=SUPER,q,killclient,

# switch window focus
bind=ALT,Tab,focusstack,next
bind=SUPER,H,focusdir,left
bind=SUPER,L,focusdir,right
bind=SUPER,K,focusdir,up
bind=SUPER,J,focusdir,down

# swap window
bind=SUPER+SHIFT,K,exchange_client,up
bind=SUPER+SHIFT,J,exchange_client,down
bind=SUPER+SHIFT,H,exchange_client,left
bind=SUPER+SHIFT,L,exchange_client,right

# switch window status
bind=SUPER,g,toggleglobal,
# bind=SUPER,Tab,toggleoverview,
bind=SUPER,Tab,spawn,noctalia msg window-switcher open
bind=SUPER,space,togglefloating,
# bind=ALT,f,togglemaximizescreen,
bind=SUPER,e,togglefullscreen,
bind=SUPER+SHIFT,f,togglemaximizescreen,
# bind=SUPER,o,toggleoverlay,
bind=SUPER,U,minimized,
bind=SUPER+SHIFT,U,restore_minimized
bind=SUPER+SHIFT,Return,toggle_scratchpad

# scroller layout
# bind=SUPER,e,set_proportion,1.0
bind=SUPER,f,switch_proportion_preset,

# switch layout
bind=SUPER+SHIFT,n,switch_layout

# tag switch
bind=SUPER,Left,focusdir,left
bind=SUPER,Right,focusdir,right
# bind=CTRL,Left,viewtoleft_have_client,0
# bind=SUPER,Right,viewtoright,0
# bind=CTRL,Right,viewtoright_have_client,0
# bind=CTRL+SUPER,Left,tagtoleft,0
# bind=CTRL+SUPER,Right,tagtoright,0

bind=SUPER,1,view,1,0
bind=SUPER,2,view,2,0
bind=SUPER,3,view,3,0
bind=SUPER,4,view,4,0
bind=SUPER,5,view,5,0

# tag: move client to the tag and focus it
# tagsilent: move client to the tag and not focus it
# bind=Alt,1,tagsilent,1

bind=SUPER+SHIFT,1,tag,1,0
bind=SUPER+SHIFT,2,tag,2,0
bind=SUPER+SHIFT,3,tag,3,0
bind=SUPER+SHIFT,4,tag,4,0

bind=SUPER+ALT,1,comboview,1
bind=SUPER+ALT,2,comboview,2
bind=SUPER+ALT,3,comboview,3
bind=SUPER+ALT,4,comboview,4

# monitor switch
# bind=alt+shift,Left,focusmon,left
# bind=alt+shift,Right,focusmon,right
# bind=SUPER+Alt,Left,tagmon,left
# bind=SUPER+Alt,Right,tagmon,right

# gaps
# bind=ALT+SHIFT,X,incgaps,1
# bind=ALT+SHIFT,Z,incgaps,-1
# bind=ALT+SHIFT,R,togglegaps

# movewin
bind=CTRL+SHIFT,Up,movewin,+0,-50
bind=CTRL+SHIFT,Down,movewin,+0,+50
bind=CTRL+SHIFT,Left,movewin,-50,+0
bind=CTRL+SHIFT,Right,movewin,+50,+0

# resizewin
bind=SUPER,Up,resizewin,+0,-50
bind=SUPER,Down,resizewin,+0,+50
bind=SUPER,minus,resizewin,-50,+0
bind=SUPER,equal,resizewin,+50,+0

# Mouse Button Bindings
# NONE mode key only work in ov mode
mousebind=SUPER,btn_left,moveresize,curmove
mousebind=SUPER+CTRL,btn_right,toggleoverview,1
mousebind=SUPER,btn_right,moveresize,curresize
# mousebind=SUPER,btn_right,moveresize,curresize
# mousebind=NONE,btn_middle,togglemaximizescreen,0

# Axis Bindings
axisbind=SUPER,UP,focusdir,up
axisbind=SUPER,DOWN,focusdir,down

# Guesture bindings
gesturebind=none,up,3,focusdir,down
gesturebind=none,down,3,focusdir,up
gesturebind=none,left,3,viewtoright
gesturebind=none,right,3,viewtoleft
gesturebind=none,up,4,toggleoverview
gesturebind=none,down,4,spawn,fuzzel

# Special binds
bindl=NONE,XF86AudioRaiseVolume,spawn,wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindl=NONE,XF86AudioLowerVolume,spawn,wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindl=NONE,XF86AudioMute,spawn,wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindl=NONE,XF86AudioMicMute,spawn,wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindl=NONE,XF86MonBrightnessUp,spawn,noctalia msg brightness-up
bindl=NONE,XF86MonBrightnessDown,spawn,noctalia msg brightness-down
# Requires playerctl
bindl=NONE,XF86AudioNext,spawn,playerctl next
bindl=NONE,XF86AudioPause,spawn,playerctl play-pause
bindl=NONE,XF86AudioPlay,spawn,playerctl play-pause
bindl=NONE,XF86AudioPrev,spawn,playerctl previous
bindl=NONE,Print,spawn,noctalia msg screenshot-region
bind=SUPER,Print,spawn,noctalia msg screenshot-fullscreen all
